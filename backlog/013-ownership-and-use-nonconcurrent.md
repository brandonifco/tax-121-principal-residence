# ownership-and-use-nonconcurrent: Ownership and use need not be concurrent

<!-- rules-factory-entry: ownership-and-use-nonconcurrent -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: blocked -->

Backlog item 13 of 23 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `ownership-and-use-nonconcurrent`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(c)
```

Evidence, verbatim:

```text
The requirements of ownership and use may be satisfied during nonconcurrent periods if both the ownership and use tests are met during the 5-year period ending on the date of the sale or exchange.
```

## Dependencies

`dependsOn` -- built before this item:

- [`ownership-and-use-aggregation`](012-ownership-and-use-aggregation.md)

## Reachability

`enabledBy` -- what the tests must set up:

- `effective-date` -- no backlog item (status: implemented)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] A hand-written handler implements `Handlers.OwnershipAndUseNonconcurrent`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `ownership-and-use-nonconcurrent` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The rule that makes the two tests independent inside one window, which is what stops an engine requiring the same 24 months for both. A test must show ownership and use in disjoint periods, both inside the five years, and one of them outside it.
```
