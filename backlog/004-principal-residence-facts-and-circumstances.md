# principal-residence-facts-and-circumstances: Which residence is the principal residence

<!-- rules-factory-entry: principal-residence-facts-and-circumstances -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: blocked -->

Backlog item 4 of 22 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `principal-residence-facts-and-circumstances`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(b)(2)
```

Evidence, verbatim:

```text
In the case of a taxpayer using more than one property as a residence, whether property is used by the taxpayer as the taxpayer's principal residence depends upon all the facts and circumstances.
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
Where a taxpayer uses more than one property as a residence, the corpus makes the answer turn on "all the facts and circumstances" and fixes neither a threshold nor a weighting over the six factors it goes on to list, which are expressly "not limited to" those six. Nobody is named to decide.
```

## Dependencies

`dependsOn` -- built before this item:

- [`residence-facts-and-circumstances`](001-residence-facts-and-circumstances.md)
- [`principal-residence-factors`](003-principal-residence-factors.md)

## Reachability

`enabledBy` -- what the tests must set up:

- `effective-date` -- no backlog item (status: implemented)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.PrincipalResidenceFactsAndCircumstances`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `principal-residence-facts-and-circumstances` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The engine can hold the six factors and the majority-of-time default and still not answer the general case, which is exactly what this entry declines. Gate 3 returns a gap for the same reason residence-facts-and-circumstances does.
```
