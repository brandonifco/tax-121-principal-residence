# out-of-residence-care: Use during periods of out-of-residence care

<!-- rules-factory-entry: out-of-residence-care -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: blocked -->

Backlog item 18 of 24 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `out-of-residence-care`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(c)(2)(ii)
```

Evidence, verbatim:

```text
(ii) Determination of use during periods of out-of-residence care. If a taxpayer has become physically or mentally incapable of self-care and the taxpayer sells or exchanges property that the taxpayer owned and used as the taxpayer's principal residence for periods aggregating at least 1 year during the 5-year period preceding the sale or exchange, the taxpayer is treated as using the property as the taxpayer's principal residence for any period of time during the 5-year period in which the taxpayer owns the property and resides in any facility (including a nursing home) licensed by a State or political subdivision to care for an individual in the taxpayer's condition.
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
The rule applies only to a taxpayer who "has become physically or mentally incapable of self-care", and the corpus states no standard for that and names nobody -- not a physician, not the taxpayer -- to determine it. Everything else in the paragraph is determinate.
```

## Dependencies

`dependsOn` -- built before this item:

- [`use-requires-occupancy`](014-use-requires-occupancy.md)
- [`ownership-and-use-test`](015-ownership-and-use-test.md)

## Reachability

`enabledBy` -- what the tests must set up:

- [`effective-date`](024-effective-date.md)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.OutOfResidenceCare`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `out-of-residence-care` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
A substitution rule with three determinate conditions -- at least one year of ownership and use in the five-year period, ownership throughout, and residence in a licensed facility -- and one open one. A test must show the one-year threshold at both boundaries and an unlicensed facility. Note that the reduced one-year requirement is a condition on the substitution and does not replace ownership-and-use-test's two years.
```
