# residence-facts-and-circumstances: Whether property is used as a residence

<!-- rules-factory-entry: residence-facts-and-circumstances -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: ready -->

Backlog item 1 of 22 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `residence-facts-and-circumstances`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(b)
```

Evidence, verbatim:

```text
Whether property is used by the taxpayer as the taxpayer's residence depends upon all the facts and circumstances.
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
The corpus makes the whole question turn on "all the facts and circumstances" and states no factor, no threshold and no weighting for this determination, and names nobody to make it. (b)(2) lists six factors for the *principal* residence question and this paragraph lists none for the prior question of whether the property is a residence at all.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- `effective-date` -- no backlog item (status: implemented)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.ResidenceFactsAndCircumstances`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `residence-facts-and-circumstances` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Phase 4 gate 3 returns a gap: the caller's own determination is not made operative and no measure is stated in the same constituent, so this is not kind: assertion (0010 -- being the only possible source is not a reason to reclassify). The engine declines and says which facts it was given.
```
