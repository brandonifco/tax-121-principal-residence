# maximum-limitation-amount: The maximum limitation amount for a combined sale

<!-- rules-factory-entry: maximum-limitation-amount -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: ready -->

Backlog item 5 of 20 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `maximum-limitation-amount`
- kind: `value`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(b)(3)(ii)
```

Evidence, verbatim:

```text
Therefore, only one maximum limitation amount of $250,000 ($500,000 for certain joint returns) applies to the combined sales or exchanges of vacant land and the dwelling unit.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- `effective-date` -- no backlog item (status: implemented)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine states this value as the evidence prints it -- every figure and unit, and where the corpus prints a finite table, the whole table.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] A hand-written handler implements `Handlers.MaximumLimitationAmount`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `maximum-limitation-amount` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
$250,000, or $500,000 for certain joint returns: the only dollar figures this section states, and it states them for the combined vacant-land sale alone. Which returns are "certain joint returns" is a fact the rule tests and a caller-supplied parameter under gate 1, fixed by section 121(b)(2) and § 1.121-2(a)(3)(i), so it gets no entry of its own; vacant-land-ordering-across-years is where that reference is answered.
```
