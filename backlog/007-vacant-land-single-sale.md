# vacant-land-single-sale: Vacant land and the dwelling unit are one sale

<!-- rules-factory-entry: vacant-land-single-sale -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: blocked -->

Backlog item 7 of 22 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `vacant-land-single-sale`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(b)(3)(ii)
```

Evidence, verbatim:

```text
(ii) Limitations—(A) Maximum limitation amount. For purposes of section 121(b)(1) and (2) (relating to the maximum limitation amount of the section 121 exclusion), the sale or exchange of the dwelling unit and the vacant land are treated as one sale or exchange.
```

## Dependencies

`dependsOn` -- built before this item:

- [`vacant-land-not-principal-residence`](006-vacant-land-not-principal-residence.md)

## Reachability

`enabledBy` -- what the tests must set up:

- `effective-date` -- no backlog item (status: implemented)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "section 121(b)(1) and (2)" -- `unmapped`: "The statutory maximum limitation amount, in a corpus that was not admitted. What this section states about it is maximum-limitation-amount."
- `cites` "the section 121 exclusion" -- `resolvedBy` [`exclusion-of-gain`](015-exclusion-of-gain.md)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] A hand-written handler implements `Handlers.VacantLandSingleSale`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `vacant-land-single-sale` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The combination rule, separate from the figure it combines against (maximum-limitation-amount) because one is a procedure and the other a value. A test must show two transactions treated as one and the same two treated apart when vacant-land-not-principal-residence is not satisfied.
```
