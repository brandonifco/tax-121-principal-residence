# principal-residence-majority-of-time: The majority-of-time default for two alternating properties

<!-- rules-factory-entry: principal-residence-majority-of-time -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: blocked -->

Backlog item 4 of 22 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `principal-residence-majority-of-time`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(b)(2)
```

Evidence, verbatim:

```text
If a taxpayer alternates between 2 properties, using each as a residence for successive periods of time, the property that the taxpayer uses a majority of the time during the year ordinarily will be considered the taxpayer's principal residence.
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
"Ordinarily will be considered" states a default and not a rule: the corpus does not say what displaces it, or on whose showing. A taxpayer who used property A for seven months and property B for five is inside the sentence, and whether the answer is A is what "ordinarily" leaves open.
```

## Dependencies

`dependsOn` -- built before this item:

- [`principal-residence-facts-and-circumstances`](003-principal-residence-facts-and-circumstances.md)

## Reachability

`enabledBy` -- what the tests must set up:

- `effective-date` -- no backlog item (status: implemented)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.PrincipalResidenceMajorityOfTime`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `principal-residence-majority-of-time` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The computable part is real and worth keeping: two properties, successive periods, a majority of the time during the year. What is unsettled is only whether the answer stands. An engine answers the majority and declines the displacement, and a test must show both a clear majority and an exact half.
```
