# no-allocation-within-dwelling-unit: No allocation within one dwelling unit

<!-- rules-factory-entry: no-allocation-within-dwelling-unit -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: blocked -->

Backlog item 18 of 20 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `no-allocation-within-dwelling-unit`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(e)
```

Evidence, verbatim:

```text
No allocation is required if both the residential and non-residential portions of the property are within the same dwelling unit.
```

## Dependencies

`dependsOn` -- built before this item:

- `dwelling-unit-definition` -- no backlog item (definedElsewhere -- the corpus does not supply the fact)
- [`allocation-required`](016-allocation-required.md)

## Reachability

`enabledBy` -- what the tests must set up:

- `effective-date` -- no backlog item (status: implemented)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] A hand-written handler implements `Handlers.NoAllocationWithinDwellingUnit`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `no-allocation-within-dwelling-unit` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The counterpart rule, and clear on its own terms once "dwelling unit" is supplied: inside one dwelling unit there is nothing to allocate. It is the reason a home office inside the house costs the taxpayer nothing under (e), while a stable does. Kept apart from allocation-required because it answers the opposite question and its answer is not open.
```
