# ownership-and-use-test: Owned and used as a principal residence for 2 of the 5 years ending on the sale

<!-- rules-factory-entry: ownership-and-use-test -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: blocked -->

Backlog item 10 of 18 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `ownership-and-use-test`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(a)
```

Evidence, verbatim:

```text
Subject to the other provisions of section 121, a taxpayer may exclude gain only if, during the 5-year period ending on the date of the sale or exchange, the taxpayer owned and used the property as the taxpayer's principal residence for periods aggregating 2 years or more.
```

## Dependencies

`dependsOn` -- built before this item:

- [`principal-residence-facts-and-circumstances`](001-principal-residence-facts-and-circumstances.md)
- `ownership-and-use-aggregation` -- no backlog item (status: implemented)
- [`use-requires-occupancy`](009-use-requires-occupancy.md)

## Reachability

`enabledBy` -- what the tests must set up:

- `effective-date` -- no backlog item (status: implemented)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "section 121" -- `unmapped`: "\"The other provisions of section 121\" are the statute, a corpus that was not admitted; which of them apply is not a rule this section states."

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] A hand-written handler implements `Handlers.OwnershipAndUseTest`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `ownership-and-use-test` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Two conditions over one window, both of which must hold for periods aggregating two years within the five years ending on the date of sale. Clear because every term of the window is fixed: a start, an end, and an aggregate. A test must show ownership without use, use without ownership, both met non-concurrently, and a period ending one day short.
```
