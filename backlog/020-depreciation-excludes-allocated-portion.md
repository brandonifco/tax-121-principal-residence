# depreciation-excludes-allocated-portion: Depreciation allocable to a non-excludable portion is not counted

<!-- rules-factory-entry: depreciation-excludes-allocated-portion -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: blocked -->

Backlog item 20 of 23 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `depreciation-excludes-allocated-portion`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(d)
```

Evidence, verbatim:

```text
Depreciation adjustments allocable to any portion of the property to which the section 121 exclusion does not apply under paragraph (e) of this section are not taken into account for this purpose.
```

## Dependencies

`dependsOn` -- built before this item:

- `depreciation-not-excludable` -- no backlog item (definedElsewhere -- the corpus does not supply the fact)
- [`allocation-required`](019-allocation-required.md)

## Reachability

`enabledBy` -- what the tests must set up:

- `effective-date` -- no backlog item (status: implemented)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "the section 121 exclusion" -- `resolvedBy` [`exclusion-of-gain`](016-exclusion-of-gain.md)
- `cites` "paragraph (e) of this section" -- `resolvedBy` [`allocation-required`](019-allocation-required.md)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] A hand-written handler implements `Handlers.DepreciationExcludesAllocatedPortion`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `depreciation-excludes-allocated-portion` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
A carve-out of the previous sentence's floor, and one of the two pointers this trial exists for: "paragraph (e) of this section" names a constituent of the same section structurally, and it resolves to the entry whose own locator is § 1.121-1(e). The pair with allocation-subject-to-depreciation is mutual and is not a cycle -- (d) excludes what (e) has already removed, and (e) subjects what it keeps to (d). A test must show a mixed-use property where the same depreciation would otherwise be counted twice.
```
