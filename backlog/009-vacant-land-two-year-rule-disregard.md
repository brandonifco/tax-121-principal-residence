# vacant-land-two-year-rule-disregard: Separate qualifying transactions and the one-sale-every-two-years rule

<!-- rules-factory-entry: vacant-land-two-year-rule-disregard -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: blocked -->

Backlog item 9 of 24 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `vacant-land-two-year-rule-disregard`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(b)(3)(ii)(B)
```

Evidence, verbatim:

```text
(B) Sale or exchange of more than one principal residence in 2-year period. If a dwelling unit and vacant land are sold or exchanged in separate transactions that qualify for the section 121 exclusion under this paragraph (b)(3), each of the transactions is disregarded in applying section 121(b)(3) (restricting the application of section 121 to only 1 sale or exchange every 2 years) to the other transactions but is taken into account as a sale or exchange of a principal residence on the date of the transaction in applying section 121(b)(3) to that transaction and the sale or exchange of any other principal residence.
```

## Dependencies

`dependsOn` -- built before this item:

- [`vacant-land-not-principal-residence`](006-vacant-land-not-principal-residence.md)
- [`vacant-land-single-sale`](007-vacant-land-single-sale.md)

## Reachability

`enabledBy` -- what the tests must set up:

- [`effective-date`](024-effective-date.md)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "the section 121 exclusion" -- `resolvedBy` [`exclusion-of-gain`](016-exclusion-of-gain.md)
- `cites` "this paragraph (b)(3)" -- `resolvedBy` [`vacant-land-not-principal-residence`](006-vacant-land-not-principal-residence.md)
- `cites` "section 121(b)(3)" -- `unmapped`: "The statutory one-sale-every-two-years limitation, in a corpus that was not admitted. Both occurrences are this one pointer."
- `cites` "of section 121 to only 1 sale or exchange every 2 years" -- `unmapped`: "The parenthetical restates the statutory limitation it has just cited; 26 U.S.C. 121 is not admitted."

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] A hand-written handler implements `Handlers.VacantLandTwoYearRuleDisregard`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `vacant-land-two-year-rule-disregard` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
A disregard rule with a direction: the other transactions do not count this one, and this one counts itself. Clear, and the sharpest test in the slice -- three transactions in a two-year window, asked about from each of the three sides.
```
