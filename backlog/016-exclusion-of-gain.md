# exclusion-of-gain: The section 121 exclusion

<!-- rules-factory-entry: exclusion-of-gain -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: blocked -->

Backlog item 16 of 23 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `exclusion-of-gain`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(a)
```

Evidence, verbatim:

```text
(a) In general. Section 121 provides that, under certain circumstances, gross income does not include gain realized on the sale or exchange of property that was owned and used by a taxpayer as the taxpayer's principal residence.
```

## Dependencies

`dependsOn` -- built before this item:

- [`ownership-and-use-test`](015-ownership-and-use-test.md)

## Reachability

`enabledBy` -- what the tests must set up:

- `effective-date` -- no backlog item (status: implemented)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "Section 121" -- `unmapped`: "The statute this section interprets. 26 U.S.C. 121 is a different corpus and is not admitted; the manifest lists it under `references`."

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] A hand-written handler implements `Handlers.ExclusionOfGain`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `exclusion-of-gain` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
What the exclusion is: gain realized on the sale or exchange of a principal residence is not gross income. The sentence states the consequence and not the conditions, which is why it depends on ownership-and-use-test rather than restating it. A test must show an excluded sale and a sale the conditions do not reach. The dollar cap is not here: this section states a maximum limitation amount only for the vacant-land case (maximum-limitation-amount), and the cap on an ordinary sale is section 121(b) and § 1.121-2, neither of them admitted.
```
