# allocation-subject-to-depreciation: The residential portion is still subject to paragraph (d)

<!-- rules-factory-entry: allocation-subject-to-depreciation -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: blocked -->

Backlog item 17 of 18 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `allocation-subject-to-depreciation`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(e)
```

Evidence, verbatim:

```text
However, section 121 does not apply to the gain allocable to the residential portion of the property to the extent provided by paragraph (d) of this section.
```

## Dependencies

`dependsOn` -- built before this item:

- `depreciation-not-excludable` -- no backlog item (definedElsewhere -- the corpus does not supply the fact)
- [`allocation-required`](014-allocation-required.md)

## Reachability

`enabledBy` -- what the tests must set up:

- `effective-date` -- no backlog item (status: implemented)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "section 121" -- `resolvedBy` [`exclusion-of-gain`](011-exclusion-of-gain.md)
- `cites` "paragraph (d) of this section" -- `resolvedBy` `depreciation-not-excludable` -- no backlog item (definedElsewhere -- the corpus does not supply the fact)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] A hand-written handler implements `Handlers.AllocationSubjectToDepreciation`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `allocation-subject-to-depreciation` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The second of the two structural pointers this trial exists for, and it runs the opposite way from depreciation-excludes-allocated-portion's: (e) qualifies itself by (d) while (d) qualifies itself by (e). Both resolve to an entry whose locator is a constituent of the same section, and neither needed a field the schema does not have.
```
