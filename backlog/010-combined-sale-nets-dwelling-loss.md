# combined-sale-nets-dwelling-loss: A loss on one of two combined transactions and the excludable gain

<!-- rules-factory-entry: combined-sale-nets-dwelling-loss -->
<!-- rules-factory-engine: Tax121PrincipalResidence; map: RulesFactory.Maps.Tax121PrincipalResidence -->
<!-- rules-factory-state: blocked -->

Backlog item 10 of 22 for Tax121PrincipalResidence, from RulesFactory.Maps.Tax121PrincipalResidence 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `combined-sale-nets-dwelling-loss`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-26-1.121-1"; citation:

```text
§ 1.121-1(b)(4) Example 4
```

Evidence, verbatim:

```text
In 2003 D sells the house and 1 acre and the 29 acres in 2 separate transactions. D sells the house and 1 acre at a loss of $25,000. D realizes $270,000 of gain from the sale of the 29 acres. D may exclude the $245,000 gain from the 2 sales.
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
Example 4 excludes $245,000 where the house and 1 acre were sold at a $25,000 loss and the 29 acres of vacant land realized $270,000 of gain: the loss is netted against the gain, and the $250,000 maximum limitation amount is not what produces the figure. No sentence of paragraph (b)(3)(ii) says that a loss on one of the two transactions reduces the gain excludable on the other. What it says is that the two are treated as one sale or exchange "For purposes of section 121(b)(1) and (2) (relating to the maximum limitation amount of the section 121 exclusion)", which is a merger bounded to the cap. The example fixes the answer for D's figures and states no rule, so the corpus determines one answer for this fact pattern and none for any other pair of figures.
```

## Dependencies

`dependsOn` -- built before this item:

- [`vacant-land-single-sale`](006-vacant-land-single-sale.md)
- [`maximum-limitation-amount`](007-maximum-limitation-amount.md)

## Reachability

`enabledBy` -- what the tests must set up:

- `effective-date` -- no backlog item (status: implemented)

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Every test of the rule sets up a state in which `effective-date` holds, and a test shows the rule does not apply where none of them holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.CombinedSaleNetsDwellingLoss`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `combined-sale-nets-dwelling-loss` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Mapped in scope against the rest of (b)(4), which stays declined, because scope is decided per rule and never per paragraph. The blind second mapping found this and the first mapping did not: every locator in the first map resolves, its extent is reached and it passes its publish gate, and no entry in it can produce $245,000. A test must show the two transactions netted and must show the engine declining on any figures but D's.
```
