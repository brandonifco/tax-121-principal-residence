{
  "effective-date": {
    "status": "implemented",
    "implementedIn": {
      "ruleset": "tax-121-principal-residence",
      "version": 1
    },
    "tests": [
      {
        "test": "EffectiveDateEntryPointTests.A_sale_on_24_December_2002_is_within_this_section_citing_1_121_1_f",
        "mutation": "Applicability.Of excluded the date itself (`saleOrExchangeDate > ApplicableFrom` for `saleOrExchangeDate >= ApplicableFrom`), so a sale on 24 December 2002 was outside the section; this test went red. It went red too with Applicability.ApplicableFrom set to the corpus's pinned baseline date (`new(2026, 1, 1)` for `new(2002, 12, 24)`), with it set a day early (`new(2002, 12, 23)`), and with SectionApplicability.Authority citing exclusion-of-gain's `§ 1.121-1(a)` (`MapEntries.ExclusionOfGain.Locator` for `MapEntries.EffectiveDate.Locator`). Citing retroactive-election's locator did NOT turn it red: both entries cite `§ 1.121-1(f)`, so the locator cannot tell them apart."
      },
      {
        "test": "EffectiveDateEntryPointTests.A_sale_on_23_December_2002_is_outside_this_section_citing_1_121_1_f",
        "mutation": "Applicability.Of found every sale within the section (`true` for `saleOrExchangeDate >= ApplicableFrom`); this test went red. It went red too with Applicability.ApplicableFrom a day early (`new(2002, 12, 23)` for `new(2002, 12, 24)`), with the finding recording the rule's date instead of the caller's (`new SectionApplicability(ApplicableFrom, ...` for `new SectionApplicability(saleOrExchangeDate, ...`), and with Authority citing exclusion-of-gain's `§ 1.121-1(a)`."
      },
      {
        "test": "EffectiveDateEntryPointTests.Without_a_sale_or_exchange_date_it_refuses_rather_than_assume_one",
        "mutation": "Handlers.EffectiveDate defaulted a missing date to the effective date (`request.SaleOrExchangeDate ?? Applicability.ApplicableFrom` for `Demand(request.SaleOrExchangeDate, ...)`), so a request with no date resolved as within the section; this test went red. It went red too with a missing date answered as an unresolved `MissingRulesData` decline citing `§ 1.121-1(f)` instead of refused."
      },
      {
        "test": "EffectiveDateEntryPointTests.The_dictionary_dispatch_carries_no_date_and_refuses_the_same_way",
        "mutation": "Handlers.EffectiveDate defaulted a missing date to the effective date (`request.SaleOrExchangeDate ?? Applicability.ApplicableFrom` for `Demand(request.SaleOrExchangeDate, ...)`), so `Registry.Resolve(\"effective-date\", RuleRequest.Empty)` resolved as within the section; this test went red. It went red too with a missing date answered as an unresolved `MissingRulesData` decline instead of refused."
      },
      {
        "test": "EffectiveDateGateTests.A_sale_on_23_December_2002_declines_OutsideCurrentScope_citing_1_121_1_f_and_naming_the_retroactive_election",
        "mutation": "Applicability.Gate let every sale proceed (`finding => true` for `finding => finding.Applies`); this test went red. It went red too with the decline's reason `MissingRulesData`, and `RequiresInterpretation`, for `OutsideCurrentScope`; with it citing the gated entry's own locator (`entry.Locator` for `finding.Authority`, § 1.121-1(a)); with the retroactive election dropped from the message; with `see § 1.121-4(j)` dropped from it; and with the gated entry's id dropped from it. Citing retroactive-election's locator (`MapEntries.RetroactiveElection.Locator` for `finding.Authority`) did NOT turn it red: both entries cite `§ 1.121-1(f)`, so the locator cannot tell them apart, and the message is what names the election."
      },
      {
        "test": "EffectiveDateGateTests.A_sale_on_24_December_2002_proceeds",
        "mutation": "Applicability.Gate declined every sale (`finding => false` for `finding => finding.Applies`); this test went red. It went red too with Applicability.Of excluding the date itself (`saleOrExchangeDate > ApplicableFrom` for `saleOrExchangeDate >= ApplicableFrom`), and with the gate evaluating the day before the effective date instead of the caller's date (`Of(ApplicableFrom.AddDays(-1))` for `Of(date)`)."
      },
      {
        "test": "EffectiveDateGateTests.A_sale_on_25_December_2002_proceeds",
        "mutation": "Applicability.Of found only the effective date itself within the section (`saleOrExchangeDate == ApplicableFrom` for `saleOrExchangeDate >= ApplicableFrom`); this test went red and every other gate and effective-date test stayed green. It went red too with Applicability.Gate declining every sale (`finding => false`), and with the gate evaluating the day before the effective date instead of the caller's date."
      },
      {
        "test": "EffectiveDateGateTests.Without_a_sale_or_exchange_date_the_gate_refuses_rather_than_assume_one",
        "mutation": "Applicability.Gate defaulted a missing date to the effective date (`saleOrExchangeDate ?? ApplicableFrom` for `?? throw new ArgumentException(...)`), so a missing date proceeded; this test went red. It went red too with the refusal's ParamName `\"saleOrExchangeDate\"` for `nameof(Requests.EffectiveDateRequest.SaleOrExchangeDate)`, and with the gated entry's id dropped from its message."
      }
    ]
  },
  "ownership-and-use-aggregation": {
    "status": "implemented",
    "implementedIn": {
      "ruleset": "tax-121-principal-residence",
      "version": 1
    },
    "tests": [
      {
        "test": "OwnershipAndUseAggregationTests.The_value_is_2_years_as_24_full_months_or_730_days_citing_1_121_1_c",
        "mutation": "TwoYears.Years set to 1 (`Years = 1` for `Years = 2`); this test went red and every other test of this entry stayed green. It went red too with TwoYears.FullMonths 23 for 24; with TwoYears.Days 729 for `365 * 2`; with TwoYears.Days a leap-year two years (`366 + 365` for `365 * 2`); and with TwoYearAggregate.Authority citing exclusion-of-gain's `§ 1.121-1(a)` (`MapEntries.ExclusionOfGain.Locator`). Citing ownership-and-use-nonconcurrent's locator (`MapEntries.OwnershipAndUseNonconcurrent.Locator`) did NOT turn it red: both entries cite `§ 1.121-1(c)`, so the locator cannot tell them apart."
      },
      {
        "test": "OwnershipAndUseAggregationTests.Twenty_four_full_months_establish_the_two_years",
        "mutation": "IsEstablishedByFullMonths excluded the threshold itself (`fullMonths > FullMonths` for `fullMonths >= FullMonths`); this test went red. It went red too with the months compared to the day count (`fullMonths >= Days`)."
      },
      {
        "test": "OwnershipAndUseAggregationTests.Twenty_three_full_months_do_not_by_months",
        "mutation": "IsEstablishedByFullMonths answered true for every count (`true` for `fullMonths >= FullMonths`); this test went red. It went red too with TwoYears.FullMonths 23 for 24."
      },
      {
        "test": "OwnershipAndUseAggregationTests.Twenty_five_full_months_establish_the_two_years",
        "mutation": "IsEstablishedByFullMonths held only at exactly the threshold (`fullMonths == FullMonths` for `fullMonths >= FullMonths`); this test went red and every other test of this entry stayed green. It went red too with the months compared to the day count (`fullMonths >= Days`)."
      },
      {
        "test": "OwnershipAndUseAggregationTests.Seven_hundred_thirty_days_establish_the_two_years",
        "mutation": "IsEstablishedByDays excluded the threshold itself (`days > Days` for `days >= Days`); this test went red. It went red too with TwoYears.Days a leap-year two years (`366 + 365` for `365 * 2`)."
      },
      {
        "test": "OwnershipAndUseAggregationTests.Seven_hundred_twenty_nine_days_do_not_by_days",
        "mutation": "IsEstablishedByDays answered true for every count (`true` for `days >= Days`); this test went red. It went red too with the days compared to the month count (`days >= FullMonths`), and with TwoYears.Days 729 for `365 * 2`."
      },
      {
        "test": "OwnershipAndUseAggregationTests.Seven_hundred_thirty_one_days_establish_the_two_years",
        "mutation": "IsEstablishedByDays held only at exactly the threshold (`days == Days` for `days >= Days`); this test went red and every other test of this entry stayed green."
      },
      {
        "test": "OwnershipAndUseAggregationTests.A_sale_on_23_December_2002_declines_OutsideCurrentScope_citing_1_121_1_f",
        "mutation": "TwoYears.For gated the effective date instead of the caller's date (`Applicability.Gate(MapEntries.OwnershipAndUseAggregation, Applicability.ApplicableFrom)` for `..., saleOrExchangeDate)`), so a 2002-12-23 sale resolved the value; this test went red. It went red too with the gate naming effective-date instead of this entry (`Gate(MapEntries.EffectiveDate, ...)`)."
      },
      {
        "test": "OwnershipAndUseAggregationTests.Without_a_sale_or_exchange_date_it_refuses_rather_than_assume_one",
        "mutation": "TwoYears.For defaulted a missing date to the effective date (`saleOrExchangeDate ?? Applicability.ApplicableFrom` for `saleOrExchangeDate`), so a request with no date resolved the value; this test went red. It went red too with the gate evaluating the effective date instead of the caller's date, and with the gate naming effective-date instead of this entry in the refusal."
      },
      {
        "test": "OwnershipAndUseAggregationTests.The_dictionary_dispatch_carries_no_date_and_refuses_the_same_way",
        "mutation": "TwoYears.For defaulted a missing date to the effective date (`saleOrExchangeDate ?? Applicability.ApplicableFrom` for `saleOrExchangeDate`), so `Registry.Resolve(\"ownership-and-use-aggregation\", RuleRequest.Empty)` resolved the value; this test went red. It went red too with the gate evaluating the effective date instead of the caller's date."
      }
    ]
  }
}
